{
  "name": "nextgen",
  "version": "0.1.0",
  "description": "nextgen from 6529",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "coverage": "npx hardhat coverage",
    "local-blockchain": "npx hardhat node",
    "lint": "solhint 'contracts/*.sol'",
    "slither": "slither . --exclude '*/',naming-convention,external-function,low-level-calls"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.18.3",
    "solhint": "^4.0.0"
  }
}